#include <stdio.h>
#include <math.h>
#define false 0
#define true 1
// is a functio to check if number is  armstrong by loop and recursion
int isArmstrong(int n);
// is a functio to check if number is  prime
int isPrime(int n);
// is a functio to check if number is  palindrom by loop and recursion
int isPalindrome(int n);
// is a functio to check if number is  strong
int isStrong(int n);